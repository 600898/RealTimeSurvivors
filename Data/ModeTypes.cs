namespace RealTimeSurvivors.Systems.Buildings
{
  /// <summary>
  /// The current state used by the BuildManager to change building mode.
  /// </summary>
  public enum ModeTypes
  {
    Select,
    Build,
    Destroy
  }
}
